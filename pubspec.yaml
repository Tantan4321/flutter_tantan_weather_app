name: flutter_tantan_weather_app
description: A simple yet elegant Flutter app for weather.

version: 1.0.0+1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  location: ^2.3.5
  flutter_bloc: ^2.1.1
  http: ^0.12.0
  equatable: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.3.0
  mockito: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
